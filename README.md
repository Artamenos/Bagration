# Багратион

Адаптивный информационный сайт спортивного клуба карате киокушин «Багратион». Проект подготовлен как статический MVP для размещения на виртуальном хостинге Timeweb.

## Что уже работает

- адаптивная главная страница для компьютеров и мобильных устройств;
- первый экран с презентацией клуба и кнопкой звонка;
- новости с перелистыванием, свайпом и автоматической сменой на мобильных устройствах;
- разделы о клубе, достижениях и возможностях учеников;
- карточки тренеров с адресами залов;
- модальные окна тренеров с биографией, расписанием и интерактивными картами;
- локальные карты OpenStreetMap без загрузки стороннего виджета при открытии страницы;
- одна общая карта с четырьмя залами для Кураш;
- переход к выбранному адресу в Яндекс Картах только по нажатию пользователя;
- календарь мероприятий с отдельными состояниями до выбора даты и для пустой даты;
- фотоальбомы;
- нижний блок с прямым звонком в клуб и подвал сайта.

Онлайн-запись и форма обратной связи убраны. Сайт не принимает и не отправляет персональные данные посетителей: связь происходит через обычную телефонную ссылку `tel:`.

## Технологии

- Next.js 16 с App Router и статическим экспортом;
- React 19;
- TypeScript;
- Tailwind CSS;
- Lucide React;
- локальные данные OpenStreetMap для интерактивных карт.

## Структура проекта

```text
app/                 страница, метаданные и глобальные стили
components/          общие компоненты и локальная интерактивная карта
sections/            основные секции главной страницы
public/              изображения, шрифты и данные карт
public/maps/         локальные данные OpenStreetMap
PROBLEMS.md           актуальный список готовности и оставшихся задач
```

Основные данные тренеров, адреса, расписания и ссылки на Яндекс Карты находятся в `sections/Coaches.tsx`. Данные локальных карт находятся в `public/maps`.

## Локальный запуск

Требуется актуальная LTS-версия Node.js.

```powershell
npm.cmd install
npm.cmd run dev
```

После запуска сайт доступен по адресу `http://localhost:3000`.

## Проверки

```powershell
npx.cmd tsc --noEmit
npm.cmd run lint
npm.cmd run build
```

Команда сборки создаёт статическую версию сайта в папке `out`. Готовую сборку можно проверить локально:

```powershell
npm.cmd run preview
```

Предпросмотр будет доступен по адресу `http://127.0.0.1:3001`.

## Размещение на Timeweb

Проект использует `output: "export"` и `trailingSlash: true` в `next.config.ts`, поэтому серверный Node.js-процесс не требуется.

1. Выполнить `npm.cmd run build`.
2. Загрузить содержимое папки `out` в корневую веб-директорию проекта на Timeweb.
3. Проверить работу страниц, изображений, карт, телефонных ссылок и модальных окон на домене.

Тестовый домен проекта: `cp29116.tw1.ru`.

### Индексация

По умолчанию поисковая индексация запрещена — это подходящий режим для тестового домена. Перед публикацией на основном домене создайте локальный файл `.env.production.local`:

```text
NEXT_PUBLIC_ALLOW_INDEXING=true
```

После этого заново выполните `npm.cmd run build`. Не включайте индексацию для тестового домена.

## Безопасность и конфиденциальность

Для текущей статической версии не нужны пароли, SMTP-доступ или серверные секреты. Файлы `.env*`, учётные данные деплоя и старый PHP-обработчик исключены через `.gitignore`. Не добавляйте такие файлы в Git.

Карты загружаются из локальных файлов проекта. Внешняя страница Яндекс Карт открывается только после явного нажатия на соответствующую ссылку.

## Текущие ограничения

- мероприятия пока не подключены к постоянному источнику данных;
- новости, тренеры, расписания и альбомы редактируются в коде;
- административная панель отсутствует;
- автоматические тесты и CI пока не настроены;
- линтер сообщает предупреждения о применении обычного `<img>` вместо `next/image`.

Актуальные выполненные и оставшиеся задачи перечислены в [PROBLEMS.md](./PROBLEMS.md).
